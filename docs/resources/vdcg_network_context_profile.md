---
page_title: "cloudavenue_vdcg_network_context_profile Resource - cloudavenue"
subcategory: "VDC Group"
description: |-
  The cloudavenue_vdcg_network_context_profile resource allows you to manage a custom (TENANT-scoped) Network Context Profile on a VDC Group. Context profiles define Layer 7 traffic criteria (application identifiers and/or domain names) that can be referenced in firewall rules via network_context_profile_ids.
---

# cloudavenue_vdcg_network_context_profile (Resource)

The `cloudavenue_vdcg_network_context_profile` resource allows you to manage a custom (TENANT-scoped) Network Context Profile on a VDC Group. Context profiles define Layer 7 traffic criteria (application identifiers and/or domain names) that can be referenced in firewall rules via `network_context_profile_ids`.

~> **Note** Network Context Profiles are TENANT-scoped and tied to the VDC Group. The profile `name` must be unique within the organisation.

~> **Sub-attribute constraint** The `sub_attributes` field inside `app_id` is only supported when `app_id.values` contains exactly **one** entry.

## Example Usage

```terraform
# Profile matching SSL traffic restricted to TLS 1.2 and 1.3
resource "cloudavenue_vdcg_network_context_profile" "example" {
  vdc_group_name = cloudavenue_vdcg.example.name
  name           = "ssl-undeprecated-only"
  description    = "Allow only TLS 1.2 and 1.3"

  app_id = {
    values = ["SSL"]
    sub_attributes = [
      {
        type   = "TLS_VERSION"
        values = ["TLS_V12", "TLS_V13"]
      }
    ]
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) The name of the Network Context Profile.

### Optional

- `app_id` (Attributes) Layer 7 App ID attribute. Defines a set of application identifiers to match. (see [below for nested schema](#nestedatt--app_id))
- `description` (String) A human-readable description of the Network Context Profile.
- `vdc_group_id` (String) <i style="color:red;font-weight: bold">(ForceNew)</i> The ID of the VDC Group. Ensure that at least one attribute from this collection is set: [vdc_group_id,vdc_group_name]. This value must start with `urn:vcloud:vdcGroup:`.
- `vdc_group_name` (String) <i style="color:red;font-weight: bold">(ForceNew)</i> The name of the VDC Group. Ensure that at least one attribute from this collection is set: [vdc_group_id,vdc_group_name].

### Read-Only

- `id` (String) The ID of the Network Context Profile.
- `scope` (String) The scope of the Network Context Profile (`SYSTEM`, `PROVIDER` or `TENANT`). Resources are always created as `TENANT`.

<a id="nestedatt--app_id"></a>
### Nested Schema for `app_id`

Required:

- `values` (Set of String) Set of application protocol identifiers for Layer 7 traffic matching. Set must contain at least 1 elements. Element value must satisfy all validations: Value must be one of: "360ANTIV" (360 Total Security antivirus traffic), "ACTIVDIR" (Microsoft Active Directory / LDAP authentication), "AMQP" (Advanced Message Queuing Protocol (message broker)), "AVAST" (Avast antivirus update traffic), "AVG" (AVG antivirus update traffic), "AVIRA" (Avira antivirus update traffic), "BDEFNDER" (Bitdefender antivirus update traffic), "BLAST" (VMware Blast display protocol (Horizon)), "CA_CERT" (Certificate Authority certificate validation (OCSP/CRL)), "CIFS" (Common Internet File System / SMB file sharing), "CLDAP" (Connectionless LDAP (UDP-based directory queries)), "CTRXCGP" (Citrix Common Gateway Protocol), "CTRXGOTO" (Citrix GoTo conferencing traffic), "CTRXICA" (Citrix Independent Computing Architecture (ICA/HDX)), "DCERPC" (Microsoft DCE/RPC distributed computing traffic), "DHCP" (Dynamic Host Configuration Protocol), "DIAMETER" (Diameter AAA protocol (successor to RADIUS, used in LTE/telecom)), "DNS" (Domain Name System), "EPIC" (Epic Systems healthcare application traffic), "ESET" (ESET antivirus update traffic), "FPROT" (F-Prot antivirus update traffic), "FTP" (File Transfer Protocol), "GITHUB" (GitHub version control and CI/CD traffic), "HTTP" (Hypertext Transfer Protocol (plain HTTP)), "HTTP2" (HTTP/2 protocol), "IMAP" (Internet Message Access Protocol (email)), "KASPRSKY" (Kaspersky antivirus update traffic), "KERBEROS" (Kerberos network authentication protocol), "LDAP" (Lightweight Directory Access Protocol), "MAXDB" (SAP MaxDB database traffic), "MCAFEE" (McAfee antivirus update traffic), "MSSQL" (Microsoft SQL Server database traffic), "MYSQL" (MySQL / MariaDB database traffic), "NFS" (Network File System), "NNTP" (Network News Transfer Protocol), "NTBIOSNS" (NetBIOS Name Service (Windows naming)), "NTP" (Network Time Protocol), "OCSP" (Online Certificate Status Protocol), "ORACLE" (Oracle database traffic), "PANDA" (Panda Security antivirus update traffic), "PCOIP" (PC-over-IP display protocol (VMware Horizon / Teradici)), "POP3" (Post Office Protocol v3 (email retrieval)), "RADIUS" (Remote Authentication Dial-In User Service), "RDP" (Microsoft Remote Desktop Protocol), "RTCP" (Real-Time Control Protocol (companion to RTP)), "RTP" (Real-Time Transport Protocol (audio/video streams)), "RTSP" (Real Time Streaming Protocol), "SIP" (Session Initiation Protocol (VoIP signalling)), "SMTP" (Simple Mail Transfer Protocol), "SNMP" (Simple Network Management Protocol), "SSH" (Secure Shell), "SSL" (SSL/TLS encrypted traffic), "SYMUPDAT" (Symantec / Norton antivirus update traffic), "SYSLOG" (Syslog event logging), "TELNET" (Telnet remote terminal protocol (unencrypted)), "TFTP" (Trivial File Transfer Protocol), "VNC" (Virtual Network Computing remote desktop), "WINS" (Windows Internet Name Service).

Optional:

- `sub_attributes` (Attributes List) Optional sub-attributes to refine the App ID match. (see [below for nested schema](#nestedatt--app_id--sub_attributes))

<a id="nestedatt--app_id--sub_attributes"></a>
### Nested Schema for `app_id.sub_attributes`

Required:

- `type` (String) The sub-attribute type used to refine the application match. Value must be one of: 
  - `TLS_VERSION` Restrict traffic by TLS protocol version.
  - `TLS_CIPHER_SUITE` Restrict traffic by TLS cipher suite.
  - `CIFS_SMB_VERSION` Restrict traffic by CIFS/SMB protocol version.
- `values` (Set of String) Allowed values for the selected sub-attribute type. Set must contain at least 1 elements. Element value must satisfy all validations: If the value of attribute <.<.type is "TLS_VERSION" the allowed values are : "TLS_V10" (TLS 1.0), "TLS_V11" (TLS 1.1), "TLS_V12" (TLS 1.2), "TLS_V13" (TLS 1.3) + If the value of attribute <.<.type is "TLS_CIPHER_SUITE" the allowed values are : "TLS_DHE_RSA_WITH_AES_128_CBC_SHA" (), "TLS_DHE_RSA_WITH_AES_128_CBC_SHA256" (), "TLS_DHE_RSA_WITH_AES_128_GCM_SHA256" (), "TLS_DHE_RSA_WITH_AES_256_CBC_SHA" (), "TLS_DHE_RSA_WITH_AES_256_CBC_SHA256" (), "TLS_DHE_RSA_WITH_AES_256_GCM_SHA384" (), "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA" (), "TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA256" (), "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256" (), "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA" (), "TLS_ECDHE_RSA_WITH_AES_256_CBC_SHA384" (), "TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384" (), "TLS_RSA_WITH_3DES_EDE_CBC_SHA" (), "TLS_RSA_WITH_AES_128_CBC_SHA" (), "TLS_RSA_WITH_AES_128_CBC_SHA256" (), "TLS_RSA_WITH_AES_128_GCM_SHA256" (), "TLS_RSA_WITH_AES_256_CBC_SHA" (), "TLS_RSA_WITH_AES_256_CBC_SHA256" (), "TLS_RSA_WITH_AES_256_GCM_SHA384" () + If the value of attribute <.<.type is "CIFS_SMB_VERSION" the allowed values are : "SMB_V1" (Legacy SMB 1.0 protocol. Deprecated and vulnerable to multiple known exploits.), "SMB_V2" (Server Message Block version 2.0.), "SMB_V3" (Server Message Block version 3.0.).

## Advanced Usage

Reference a Network Context Profile in a VDC Group Firewall Rule for Layer 7 filtering:

```terraform
resource "cloudavenue_vdcg_network_context_profile" "ssl_strict" {
  vdc_group_name = cloudavenue_vdcg.example.name
  name           = "ssl-undeprecated-only"
  description    = "Allow only TLS 1.2 and 1.3"

  app_id = {
    values = ["SSL"]
    sub_attributes = [
      {
        type   = "TLS_VERSION"
        values = ["TLS_V12", "TLS_V13"]
      }
    ]
  }
}

resource "cloudavenue_vdcg_firewall" "example" {
  vdc_group_name = cloudavenue_vdcg.example.name
  rules = [
    {
      name        = "allow outbound strict SSL"
      action      = "ALLOW"
      direction   = "OUT"
      ip_protocol = "IPV4"

      network_context_profile_ids = [cloudavenue_vdcg_network_context_profile.ssl_strict.id]
    }
  ]
}

```

## Import

Import is supported using the following syntax:
```shell
terraform import cloudavenue_vdcg_network_context_profile.example vdcGroupIDOrName.profileIDOrName
```
